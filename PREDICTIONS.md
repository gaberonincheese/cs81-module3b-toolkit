PREDICTIONS

function double -- My prediction is that the user will be prompted to give a number and will get the number doubled in return.

function square -- My prediction is that the user will be prompt to give a number and will get the number squared in return.

function isEven -- My prediction is that users will be able to get an accurate determination as to whether or not a number is even due to use of the modulo to see if a remainder remains.

function isOdd -- My prediction is that users will be able to get an accurate determination as to whether or not a number is odd due to use of the modulo to see if a remainder remains.

function multiply -- My prediction is that the function will take the two inputted values and return the resulting multiplied number. I'm curious to see what happens if I provide valid + invalid inputs.  