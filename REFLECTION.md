REFLECTION

What patterns or surprises did you notice?

This exercise reinforced my understanding that the computer works with what you give it. In many instances, I noticed that when the user input invalid values (such as letters or words instead of numbers), the code would execute without further consideration. One such surprising moment for me was the isOdd function, which would return true even if a letter / word was input because it was only determining if the final value was not 0. 

What logic challenged your thinking?

At this point, I didn't encounter any logic that was particularly challenging. The most challenge I've faced is adjusting to JavaScript's syntax considering my background in C++.

How might this kind of toolkit be used in the real world?

Having a readily accessible math toolkit would be an invaluable resource in order to save time when doing calculations and make more advanced calculations easier to parse. 